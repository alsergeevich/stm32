# Result Handler - Библиотека для обработки результатов в стиле Rust

Библиотека для безопасной обработки результатов функций в языке C, вдохновленная типом `Result<T, E>` из Rust.

## 📋 Содержание

- [Описание](#описание)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [API](#api)
- [Примеры использования](#примеры-использования)
- [Управление памятью](#управление-памятью)
- [Ограничения](#ограничения)

## 📖 Описание

`Result Handler` предоставляет тип `Result`, который позволяет функциям возвращать либо успешный результат, либо сообщение об ошибке. Это помогает избежать использования магических значений (например, `-1` для ошибок) и делает обработку ошибок более явной и безопасной.

### Основные возможности:

- ✅ Автоматическое выделение памяти для значений
- ✅ Безопасное копирование данных
- ✅ Явная обработка ошибок
- ✅ Простой и понятный API
- ✅ Защита от утечек памяти

## 🔧 Установка

Просто скопируйте файлы `resultHandler.h` и `resultHandler.c` в ваш проект и подключите заголовочный файл:

```c
#include "resultHandler.h"
```

### Компиляция

```bash
make          # Собрать проект
make run      # Собрать и запустить
make clean    # Очистить скомпилированные файлы
```

## 🚀 Быстрый старт

```c
#include "resultHandler.h"
#include <stdio.h>

Result divide(int a, int b) {
    if (b == 0) {
        return ERR("Division by zero");
    }
    double result = (double)a / b;
    return OK(result);
}

int main() {
    Result res = divide(10, 2);
    double *value = UNWRAP(&res, double);
    
    if (value != NULL) {
        printf("Result: %f\n", *value);
    } else {
        printf("Error: %s\n", res.Err_message);
    }
    
    result_free(&res);
    return 0;
}
```

## 📚 API

### Структура `Result`

```c
typedef struct Result {
    void *Ok_value;        // Указатель на успешный результат
    char Err_message[50];  // Сообщение об ошибке
    bool is_Ok;            // Флаг успешности операции
} Result;
```

### Макросы

#### `OK(value)`

Создает успешный результат с автоматическим выделением памяти и копированием значения.

**Параметры:**
- `value` - значение любого типа для копирования

**Возвращает:**
- `Result` с `is_Ok = true`

**Пример:**
```c
int number = 42;
Result res = OK(number);
```

#### `ERR(message)`

Создает результат с ошибкой.

**Параметры:**
- `message` - строковый литерал с сообщением об ошибке

**Возвращает:**
- `Result` с `is_Ok = false`

**Пример:**
```c
Result res = ERR("File not found");
```

#### `UNWRAP(result, type)`

Безопасно извлекает значение из `Result`.

**Параметры:**
- `result` - указатель на структуру `Result`
- `type` - тип значения для извлечения

**Возвращает:**
- Указатель на значение, если `is_Ok == true`
- `NULL`, если `is_Ok == false`

**Пример:**
```c
Result res = OK(3.14);
double *value = UNWRAP(&res, double);
if (value != NULL) {
    printf("%f\n", *value);
}
```

### Функции

#### `void result_free(Result *result)`

Освобождает память, выделенную для `Result`.

**Параметры:**
- `result` - указатель на структуру `Result` (может быть `NULL`)

**Примечания:**
- Безопасно вызывать для результатов с ошибкой
- Безопасно вызывать с `NULL` указателем
- После вызова `Ok_value` устанавливается в `NULL`

**Пример:**
```c
Result res = OK(42);
// ... использование ...
result_free(&res);
```

## 💡 Примеры использования

### Пример 1: Деление чисел

```c
Result divide(int a, int b) {
    if (b == 0) {
        return ERR("Division by zero");
    }
    double result = (double)a / b;
    return OK(result);
}
```

### Пример 2: Работа с разными типами

```c
// С целыми числами
Result get_int() {
    int value = 100;
    return OK(value);
}

// С числами с плавающей точкой
Result get_double() {
    double value = 3.14159;
    return OK(value);
}

// С пользовательскими структурами
typedef struct {
    int x, y;
} Point;

Result get_point() {
    Point p = {10, 20};
    return OK(p);
}
```

### Пример 3: Цепочка операций

```c
Result process_data(int input) {
    if (input < 0) {
        return ERR("Negative input not allowed");
    }
    
    double processed = input * 2.5;
    return OK(processed);
}

int main() {
    Result res = process_data(10);
    double *value = UNWRAP(&res, double);
    
    if (value) {
        printf("Processed: %f\n", *value);
    } else {
        printf("Error: %s\n", res.Err_message);
    }
    
    result_free(&res);
    return 0;
}
```

### Пример 4: Обработка множественных результатов

```c
int main() {
    Result results[3];
    
    results[0] = divide(10, 2);
    results[1] = divide(20, 4);
    results[2] = divide(5, 0);
    
    for (int i = 0; i < 3; i++) {
        double *val = UNWRAP(&results[i], double);
        if (val) {
            printf("Result %d: %f\n", i, *val);
        } else {
            printf("Result %d: Error - %s\n", i, results[i].Err_message);
        }
        result_free(&results[i]);
    }
    
    return 0;
}
```

## 🧠 Управление памятью

### Важные правила:

1. **Всегда вызывайте `result_free()`** после использования `Result`, созданного через `OK()`
2. **Память выделяется автоматически** при использовании макроса `OK()`
3. **Безопасно вызывать `result_free()`** для результатов с ошибкой
4. **Не освобождайте память вручную** - используйте только `result_free()`

### Правильный паттерн использования:

```c
Result res = some_function();
double *value = UNWRAP(&res, double);
if (value != NULL) {
    // Используем value
    printf("%f\n", *value);
} else {
    // Обрабатываем ошибку
    printf("Error: %s\n", res.Err_message);
}
result_free(&res);  // ВСЕГДА освобождаем память!
```

### ❌ Неправильное использование:

```c
// НЕПРАВИЛЬНО: забыли освободить память
Result res = OK(42);
double *val = UNWRAP(&res, double);
// Утечка памяти!

// НЕПРАВИЛЬНО: двойное освобождение
Result res = OK(42);
free(res.Ok_value);  // Не делайте так!
result_free(&res);   // result_free уже освободит память
```

## ⚠️ Ограничения

1. **Размер сообщения об ошибке**: ограничен 49 символами (50 с нулевым терминатором)
2. **Типы данных**: работает с любыми типами, но для больших структур может быть неэффективно
3. **Память**: требует вызова `result_free()` для каждого успешного результата
4. **NULL указатели**: `UNWRAP` возвращает `NULL` для ошибок, всегда проверяйте результат

## 🔍 Отладка

Для отладки можно проверить состояние `Result`:

```c
Result res = some_function();

if (res.is_Ok) {
    printf("Success!\n");
    // Используем UNWRAP для безопасного доступа
} else {
    printf("Error: %s\n", res.Err_message);
}

result_free(&res);
```

## 📝 Лицензия

Этот код предоставляется "как есть" для использования в ваших проектах.

## 🤝 Вклад

Если вы нашли ошибку или хотите предложить улучшение, создайте issue или pull request.

---

**Примечание**: Эта библиотека вдохновлена типом `Result<T, E>` из языка Rust и адаптирована для использования в C.

